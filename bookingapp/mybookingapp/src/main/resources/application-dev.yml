
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mybookingapp_m
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jdbc:
    template:
      fetch-size: 100

  application:
    name: mybookingapp

server:
  port: 9090